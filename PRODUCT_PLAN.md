# AI Agent 研发编排平台产品方案

## 1. 文档信息

- 文档名称：AI Agent 研发编排平台产品方案
- 文档版本：v2.0
- 创建日期：2026-03-14
- 更新日期：2026-03-15
- 文档用途：作为产品设计、系统架构、研发排期、测试验收的统一基线
- 文档状态：待评审

## 2. 产品概述

本产品不是传统意义上的项目管理工具，而是一个面向软件研发交付的 AI Agent 编排平台。

平台的核心目标是把“需求 -> 迭代计划 -> 角色分工 -> 文档产出 -> 开发执行 -> 测试验证 -> 发布准备”这一条研发链路，交给一组可管理、可替换、可协同的 sub-agent 来完成。

每个 sub-agent 对应一个研发角色，例如：

- 产品经理 Agent
- 设计师 Agent
- 开发 Agent
- 测试 Agent
- 发布配置 Agent

每个 Agent 都可以绑定到底层执行提供方，例如：

- Codex
- Claude Code
- 未来扩展的其他代码或任务执行 Agent

平台负责做三件核心事情：

1. 把需求自动拆解成可执行的迭代计划。
2. 把每轮迭代按角色拆解为多个 Agent 任务并驱动协作。
3. 统一管理 Agent 的配置、执行、交接、产物和状态。

## 3. 产品定位

### 3.1 产品定义

该产品是一个单用户优先的 AI 研发编排平台，用于让一个操作者同时管理多个专业角色 sub-agent，并通过统一编排机制驱动这些 Agent 完成软件研发全流程工作。

### 3.2 核心价值

- 将“需求拆解为迭代计划”从人工动作变成平台内核心能力。
- 将“产品、设计、开发、测试、发布”角色抽象为可配置、可替换的 Agent。
- 将 Codex、Claude Code 等执行器统一接入到同一个平台模型中。
- 让 Agent 协作过程可追踪、可审计、可重试、可人工干预。
- 让每轮迭代天然沉淀开发文档、接口文档、测试用例、测试报告和代码提交记录。

### 3.3 目标用户

- 当前阶段：单个操作者管理多个 AI 角色 Agent 的研发团队
- 下一阶段：小型人机协同团队
- 未来阶段：多项目、多团队、多 Agent 编排平台

## 4. 产品原则

### 4.1 设计原则

1. Agent 编排优先于传统项目管理。
2. 需求拆解与迭代规划是核心产品能力，不是附属功能。
3. 人始终保留审批和纠偏权。
4. Agent 必须有明确角色边界和交接产物。
5. 所有执行动作都必须可追踪、可审计、可回放。

### 4.2 单用户优先原则

首版默认只有一个真实操作者，因此首版不做复杂的人类角色权限系统。产品中的“角色”主要指 Agent 角色，而不是人类账号角色。

首版要求：

1. 一个用户可以创建项目、配置 Agent、发起编排、审核输出。
2. 所有 Agent 输出默认可由当前用户查看和确认。
3. 复杂的人类成员协作、组织权限和审批流不纳入 MVP。

### 4.3 编排原则

Agent 协作不能只靠自由聊天或临时 prompt 串联，必须有明确编排机制。

平台采用以下原则：

1. 每次执行必须有一个明确的编排 Run。
2. 每个 Agent 的输入、输出、状态、失败原因都必须记录。
3. Agent 之间通过结构化交接件协作，不直接依赖上下文猜测。
4. 关键阶段必须支持人工确认后再进入下一阶段。

## 5. 核心场景

### 5.1 场景一：从 PRD 自动生成迭代计划

1. 用户创建项目并录入 PRD。
2. 平台调用产品经理 Agent 分析需求。
3. Agent 输出迭代拆解建议，包括：
   - 迭代目标
   - 迭代优先级
   - 每轮范围边界
   - 每轮交付物
   - 每轮风险点
4. 用户审核后生成正式迭代计划。

### 5.2 场景二：按迭代启动多角色 Agent 协作

1. 用户选择某轮迭代并发起执行。
2. 编排器依次触发产品经理、设计师、开发、测试、发布配置 Agent。
3. 各 Agent 基于上游交接件生成各自产物。
4. 所有产物沉淀到平台并关联到该轮迭代。

### 5.3 场景三：通过 Codex / Claude Code 执行开发任务

1. 开发 Agent 绑定到底层执行器。
2. 编排器为开发 Agent 下发结构化任务。
3. 执行器返回执行日志、产出摘要、代码变更结果。
4. 平台记录执行过程，并驱动测试 Agent 进入下一阶段。

### 5.4 场景四：Agent 可管理、可替换、可增删

1. 用户可以新增一个 Agent。
2. 用户可以删除或停用一个 Agent。
3. 用户可以修改 Agent 角色、底层执行器、提示词模板、可处理任务类型。
4. 用户可以决定某轮迭代由哪些 Agent 参与执行。

## 6. 核心产品能力

### 6.1 需求拆解与迭代规划

这是 MVP 的核心功能之一。

平台必须支持：

- 输入 PRD、需求文档、补充约束
- 调用产品经理 Agent 输出迭代计划草稿
- 将需求拆解为：
  - 迭代
  - 工作包
  - 角色职责
  - 验收标准
  - 风险项
- 支持人工修改迭代计划并确认落地

### 6.2 多角色 sub-agent 管理

这是 MVP 的核心功能之一。

平台必须支持以下 Agent 角色模板：

- 产品经理 Agent
- 设计师 Agent
- 开发 Agent
- 测试 Agent
- 发布配置 Agent

平台必须支持：

- Agent 列表
- Agent 创建
- Agent 编辑
- Agent 停用 / 删除
- Agent 绑定角色模板
- Agent 绑定执行提供方
- Agent 配置系统提示词 / 任务模板

### 6.3 Agent 执行提供方适配

平台需要抽象 Provider Adapter 层，统一接入不同执行器。

MVP 先支持：

- Codex Adapter
- Claude Code Adapter

每个 Adapter 至少具备：

- 连接配置
- 调用能力
- 输入输出封装
- 执行日志采集
- 失败状态回传

### 6.4 Agent 编排与协同

平台需要一个中央编排器来管理多 Agent 工作流。

它负责：

- 接收迭代执行请求
- 按阶段分配给不同 Agent
- 管理前后置依赖
- 记录 Agent 状态
- 收集交付物
- 失败重试
- 进入人工确认节点

### 6.5 交付物与证据链管理

每轮迭代必须沉淀结构化交付物，包括但不限于：

- 迭代概述
- 开发文档
- 接口文档
- 测试用例
- 测试报告
- 发布配置说明
- 执行日志
- 代码提交记录

这些交付物必须可追溯到：

- 哪个项目
- 哪轮迭代
- 哪个 Agent
- 哪次执行
- 基于哪个输入生成

## 7. MVP 重新定义

### 7.1 MVP 目标

MVP 不是做一个“大而全”的研发平台，而是先验证如下闭环：

1. 用户可以录入需求并生成迭代计划。
2. 用户可以创建、删除、配置多角色 Agent。
3. 平台可以按一轮迭代驱动多个 Agent 顺序协作。
4. 开发 Agent 能通过 Codex 或 Claude Code 执行任务。
5. 测试 Agent 和发布配置 Agent 能产出文档和检查结果。
6. 每轮迭代能沉淀完整文档和执行记录。

### 7.2 MVP 必做模块

1. 基础账号与系统配置
2. 项目与工作区管理
3. 需求中心
4. 迭代规划中心
5. Agent 注册中心
6. Provider 适配与配置
7. 编排中心
8. 交付物中心
9. 基础执行看板

### 7.3 MVP 延后模块

- 多人协作权限系统
- 多组织与多租户
- 自动合并 PR
- 自动生产环境发布
- 自定义复杂工作流拖拽编排
- 完整通知中心

## 8. 角色 Agent 体系设计

### 8.1 产品经理 Agent

职责：

- 分析 PRD
- 输出需求拆解
- 生成迭代计划
- 为其他 Agent 生成目标、范围、验收标准

主要输出：

- 迭代计划
- 工作包列表
- 验收标准
- 风险提示

### 8.2 设计师 Agent

职责：

- 分析需求与迭代目标
- 输出页面结构、交互说明、设计约束
- 生成设计交接件

主要输出：

- 页面清单
- 信息架构
- 交互描述
- 设计说明文档

### 8.3 开发 Agent

职责：

- 基于产品和设计交接件输出开发方案
- 产出开发文档和接口文档
- 调用 Codex 或 Claude Code 执行开发任务

主要输出：

- 开发文档
- 接口文档
- 代码变更结果
- 实现说明

### 8.4 测试 Agent

职责：

- 根据需求、设计、代码变更生成测试用例
- 产出测试计划和测试报告
- 验证核心流程是否通过

主要输出：

- 测试用例文档
- 测试报告
- 风险与缺陷摘要

### 8.5 发布配置 Agent

职责：

- 检查配置项和发布前条件
- 输出部署说明、环境变量清单、发布检查项
- 产出发布准备结果

主要输出：

- 发布配置文档
- 环境清单
- 发布检查报告

## 9. 编排与协作方案

### 9.1 推荐方案

推荐采用“编排引擎 + 事件总线 + 状态存储”的混合方案，而不是只用总线。

原因：

- 只用总线适合解耦，不适合控制复杂阶段状态。
- 只用工作流引擎会让 Agent 间异步事件处理不够灵活。
- 混合方案既能做阶段控制，也能做异步重试和日志追踪。

### 9.2 方案组成

#### 1. Orchestrator

中央编排器，负责：

- 创建 Run
- 生成阶段任务
- 控制依赖顺序
- 处理失败、重试、暂停、继续

#### 2. Event Bus

事件总线，负责在系统内部传播状态变化。

典型事件：

- `requirement.submitted`
- `plan.generated`
- `iteration.started`
- `agent.task.assigned`
- `artifact.generated`
- `approval.requested`
- `agent.task.failed`
- `run.completed`

#### 3. State Store

持久化记录：

- 当前 Run 到哪个阶段
- 哪些 Agent 已完成
- 哪些 Agent 失败
- 哪些交付物已生成
- 哪些审批节点未通过

#### 4. Artifact Store

用于统一保存所有迭代产物、执行日志和结构化输出。

### 9.3 MVP 编排模式

MVP 先采用固定阶段编排：

1. 需求分析
2. 迭代规划
3. 设计交付
4. 开发交付
5. 测试交付
6. 发布准备

每个阶段默认由一个主 Agent 负责，支持人工确认后进入下一阶段。

### 9.4 交接机制

Agent 之间不能只靠自然语言长上下文传递信息，而应通过结构化交接件传递。

交接件最少包含：

- 来源阶段
- 来源 Agent
- 目标 Agent
- 输入摘要
- 关键约束
- 产出链接
- 风险提醒

## 10. 核心业务流程

### 10.1 项目启动流程

1. 用户创建项目和工作区。
2. 配置代码仓库、文档位置、执行环境说明。
3. 配置可用 Provider，例如 Codex 或 Claude Code。
4. 创建或启用默认 Agent 角色模板。

### 10.2 需求到迭代计划流程

1. 用户提交 PRD 或需求文档。
2. 平台触发产品经理 Agent 分析需求。
3. Agent 输出迭代计划草稿。
4. 用户审核并生成正式迭代计划。

### 10.3 迭代执行流程

1. 用户选择某轮迭代并发起 Run。
2. 编排器按阶段分配任务。
3. 各 Agent 依次输出文档、代码或检查结果。
4. 阶段失败时可重试、跳过或人工改写后继续。
5. 所有产物归档到该轮迭代。

### 10.4 交付归档流程

1. 迭代完成后，平台自动汇总产物。
2. 形成该轮交付包：
   - README
   - development.md
   - api.md
   - test-cases.md
   - test-report.md
   - release.md
3. 记录代码提交、测试结果、执行日志和异常信息。

## 11. 信息架构

建议首版导航结构如下：

- 工作台
- 项目中心
- 需求中心
- 迭代规划
- Agent 中心
- 编排运行中心
- 交付物中心
- 执行记录
- 系统设置

项目详情页建议包含：

- 概览
- 需求
- 迭代计划
- Runs
- Agents
- Artifacts
- Integrations
- Logs

## 12. 功能模块详细方案

### 12.1 基础账号与系统配置

MVP 功能：

- 单账号初始化
- 基础登录
- Provider 配置
- 工作目录配置
- 系统日志

### 12.2 项目与工作区管理

MVP 功能：

- 创建项目
- 配置项目代码仓库
- 配置文档根目录
- 配置默认执行工作区
- 配置项目技术栈和运行方式

### 12.3 需求中心

MVP 功能：

- 录入 PRD / 需求文档
- 管理需求版本
- 标记业务目标、约束、验收条件
- 将需求提交给产品经理 Agent

### 12.4 迭代规划中心

这是 MVP 核心模块。

MVP 功能：

- 展示需求拆解结果
- 生成迭代计划草稿
- 编辑迭代标题、目标、范围、依赖
- 生成每轮角色工作包
- 确认并冻结正式计划

### 12.5 Agent 中心

这是 MVP 核心模块。

MVP 功能：

- 内置 5 个角色模板
- 新增 Agent
- 编辑 Agent
- 删除 Agent
- 停用 / 启用 Agent
- 绑定 Provider
- 配置 Agent 提示词模板
- 配置 Agent 可处理任务类型

### 12.6 Provider 适配中心

MVP 功能：

- 配置 Codex 适配器
- 配置 Claude Code 适配器
- 维护连接状态
- 维护 Provider 能力说明
- 查看最近执行记录

### 12.7 编排运行中心

这是 MVP 核心模块。

MVP 功能：

- 发起 Run
- 查看阶段状态
- 查看当前由哪个 Agent 处理
- 人工暂停 / 继续 / 重试
- 人工确认后进入下一阶段
- 查看失败原因

### 12.8 交付物中心

MVP 功能：

- 按项目查看交付物
- 按迭代查看交付物
- 按 Agent 查看交付物
- 查看文档版本
- 关联 Git 提交和测试结果

### 12.9 执行记录中心

MVP 功能：

- 查看 Agent 执行日志
- 查看阶段切换记录
- 查看输入输出摘要
- 查看失败和重试记录

## 13. 数据模型建议

建议首版使用 PostgreSQL 作为主数据库，核心实体如下：

- User
- Project
- Workspace
- Requirement
- RequirementVersion
- IterationPlan
- Iteration
- WorkPackage
- AgentTemplate
- AgentInstance
- ProviderConfig
- OrchestrationRun
- RunStage
- AgentTask
- Handoff
- Artifact
- ExecutionLog
- BuildRecord
- TestReport
- AuditLog

### 13.1 关键关系

- 一个 Project 包含多个 Requirement、IterationPlan、Iteration、AgentInstance、OrchestrationRun、Artifact。
- 一个 Requirement 可以生成多个 IterationPlan 草稿，但只有一个被确认。
- 一个 Iteration 包含多个 WorkPackage。
- 一个 WorkPackage 可以分配给某个 AgentInstance。
- 一个 OrchestrationRun 包含多个 RunStage。
- 一个 RunStage 可以触发多个 AgentTask。
- 一个 AgentTask 会生成一个或多个 Artifact。
- 一个 Handoff 用于连接两个阶段或两个 AgentTask。

### 13.2 数据库设计原则

- 主实体字段稳定，扩展字段使用 JSONB。
- 所有执行相关对象保留状态机字段。
- 所有重要实体保留 `created_by`、`updated_at`、`version`。
- 所有 Run 和 Agent 执行都必须保留原始输入摘要和输出摘要。

## 14. API 边界建议

首版 API 建议按以下资源组织：

- `/auth`
- `/projects`
- `/workspaces`
- `/requirements`
- `/iteration-plans`
- `/iterations`
- `/agents/templates`
- `/agents/instances`
- `/providers`
- `/runs`
- `/runs/:id/stages`
- `/agent-tasks`
- `/handoffs`
- `/artifacts`
- `/execution-logs`
- `/build-records`
- `/test-reports`
- `/audit-logs`

## 15. 推荐技术方案

### 15.1 技术栈

- 前端：React + TypeScript + Vite
- 后端：Node.js + NestJS
- 数据库：PostgreSQL
- 队列 / 异步任务：Redis + BullMQ
- 文档存储：本地文件系统抽象层，后续可切对象存储
- 执行器适配：Provider Adapter Layer
- 前端状态：TanStack Query + Zustand

### 15.2 编排技术建议

MVP 推荐如下实现：

- Orchestrator：NestJS 内部编排服务
- Event Bus：BullMQ 事件或 Redis Streams
- State Store：PostgreSQL
- Artifact Store：数据库元数据 + 本地文件落盘

原因：

- 实现成本低
- 易于本地开发
- 具备基本重试、异步和审计能力

V2 再考虑：

- Temporal
- NATS / Kafka
- 更灵活的可视化工作流引擎

### 15.3 Provider 接入方式

每个 Provider Adapter 负责统一实现以下接口：

- `validateConfig`
- `startTask`
- `pollTask`
- `cancelTask`
- `normalizeOutput`
- `collectLogs`

这样 Agent 不直接依赖具体执行器品牌。

## 16. MVP 开发计划

### 16.1 迭代阶段

#### 阶段 A：工程底座与 Provider 配置

- 项目骨架
- 单用户账号
- Provider 配置能力
- 工作区配置
- 基础日志能力

#### 阶段 B：需求中心与迭代规划

- Requirement 模型
- 需求录入
- 需求版本
- 迭代计划生成草稿
- 迭代计划确认

#### 阶段 C：Agent 中心

- AgentTemplate
- AgentInstance
- Agent CRUD
- Provider 绑定
- 角色模板配置

#### 阶段 D：编排中心

- Run 模型
- 阶段状态机
- Handoff 模型
- 固定阶段编排
- 人工确认节点

#### 阶段 E：交付物与执行闭环

- Artifact 模型
- 交付物归档
- 测试报告
- 发布配置文档
- Git 提交记录关联

#### 阶段 F：MVP 收口

- 全链路联调
- 失败重试
- 日志补全
- 验收场景走通

## 17. MVP 验收标准

满足以下条件即可认为 MVP 达到可用标准：

1. 可以录入需求并生成迭代计划草稿。
2. 可以确认正式迭代计划。
3. 可以创建、编辑、删除、停用 Agent。
4. Agent 可以绑定 Codex 或 Claude Code。
5. 可以发起一轮迭代 Run。
6. 平台可以按固定阶段驱动多个 Agent 协同。
7. 可以为每轮迭代沉淀开发文档、接口文档、测试用例、测试报告、发布配置文档。
8. 失败状态可见且可以重试。
9. 所有 Agent 执行记录和交付物可追溯。

## 18. 核心验收场景

- 用户提交一个 PRD，平台生成 3 到 6 轮迭代计划草稿。
- 用户确认其中一版计划后，平台生成正式迭代列表。
- 用户为某个项目启用产品经理、设计师、开发、测试、发布配置 5 个 Agent。
- 开发 Agent 可以绑定 Codex，测试 Agent 可以绑定 Claude Code。
- 用户发起某轮迭代，平台依次触发多个 Agent 并沉淀完整交付物。
- 平台能够看到某个阶段失败、重试和最终完成的完整记录。

## 19. 产品指标建议

### 19.1 业务指标

- 需求到迭代计划生成成功率
- 迭代计划采纳率
- Agent 执行成功率
- 单轮迭代完整交付率
- 交付物完整率

### 19.2 效率指标

- 从需求提交到计划生成的耗时
- 单轮迭代从启动到完成的耗时
- Agent 平均等待时长
- Agent 失败后的平均恢复时长

## 20. 风险与对策

### 20.1 产品风险

- 风险：把平台做成传统项目管理工具，导致核心差异性丢失
- 对策：坚持以“需求拆解 + Agent 编排”作为产品主线

### 20.2 技术风险

- 风险：不同 Provider 行为差异大
- 对策：统一通过 Adapter 层隔离差异

### 20.3 编排风险

- 风险：只靠消息总线导致状态难以控制
- 对策：采用“编排器 + 总线 + 状态存储”的混合方案

### 20.4 交付风险

- 风险：Agent 输出很多，但缺乏结构化交付物
- 对策：强制要求每个阶段产出标准化 Artifact

## 21. 后续产出建议

基于本方案，下一步建议继续输出：

1. Agent 角色模板定义文档
2. 编排状态机设计文档
3. Provider Adapter 技术设计文档
4. MVP 数据库 ER 图
5. MVP API 设计文档
6. MVP 迭代执行计划

## 22. 结论

这次产品重设计后的核心变化是：

- “需求拆解为迭代计划”从辅助能力升级为核心能力
- “产品经理、设计师、开发、测试、发布配置”从人类角色转为可配置的 sub-agent
- “Agent 可添加、删除、绑定执行器、参与编排”进入 MVP 主体
- “编排器 + 事件总线 + 状态存储”作为首版推荐架构

该产品的本质不再是项目管理平台，而是一个以软件交付为目标的 AI Agent 编排平台。
